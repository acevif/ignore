# TODO

## Rust (`rust/`)

- [x] Implement `ignore --version` / `ignore version`
- [x] Implement `ignore help`
- [ ] Port the shell behavior: generate `.gitignore` from `Ignorefile`
- [ ] Add an end-to-end test that runs `ignore` and asserts the generated `.gitignore`
